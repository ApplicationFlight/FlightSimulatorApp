<UserControl x:Class="FlightSimulatorApp.View.UserControls.VideoPlayer"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:FlightSimulatorApp.View.UserControls"
      mc:Ignorable="d" Height="88.3" Width="453.2">
    <Grid x:Name="text" Background="#FFB9B2B2" Margin="0,3,-0.2,-1" Height="86" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
        </Grid.ColumnDefinitions>
        <Button x:Name="Play" Content="Play"  HorizontalAlignment="Left" Margin="8,10,0,0" VerticalAlignment="Top" Width="145" Click="Play_Click" Height="30" FontSize="18" d:LayoutOverrides="VerticalAlignment"/>
        <Button x:Name="Pause" Content="Pause" HorizontalAlignment="Left" Margin="146,10,0,0" VerticalAlignment="Top" Width="145" Click="Pause_Click" Height="30" FontSize="18" d:LayoutOverrides="VerticalAlignment"/>
        <Slider x:Name="Slider_Time" Value="{Binding VM_Percentage}" Margin="99,52,90,0" VerticalAlignment="Top" Height="23" Maximum="100" Minimum="0" TickFrequency="1" d:LayoutOverrides="HorizontalAlignment" />
        <!-- Button Content="Speed" HorizontalAlignment="Left" Margin="331,562,0,0" VerticalAlignment="Top" Width="145" Height="30" FontSize="18"/-->
        <ComboBox x:Name="Speed"  HorizontalAlignment="Left" Margin="315,10,0,0" VerticalAlignment="Top" Width="109" Height="30" FontSize="18" SelectionChanged="Speed_SelectionChanged" RenderTransformOrigin="0.864,-2.542" d:LayoutOverrides="VerticalAlignment">
            <ComboBoxItem x:Name="_05_Speed" Content="x0.5" HorizontalAlignment="Right" Margin="0,0,0.2,0" Width="107"/>
            <ComboBoxItem x:Name="_10_Speed" IsSelected="True" Content="x1.0"/>
            <ComboBoxItem x:Name="_15_Speed" Content="x1.5"/>
        </ComboBox>
        <TextBlock x:Name="Overall_Time" HorizontalAlignment="Right" Margin="0,48,357,0" TextWrapping="Wrap" Text="{Binding VM_Relative_time}" VerticalAlignment="Top" RenderTransformOrigin="0.5,0.5" Height="28" Width="87" FontSize="20" d:LayoutOverrides="LeftMargin, RightMargin">
            <TextBlock.RenderTransform>
                <TransformGroup>
                    <ScaleTransform/>
                    <SkewTransform/>
                    <RotateTransform Angle="719.45"/>
                    <TranslateTransform/>
                </TransformGroup>
            </TextBlock.RenderTransform>
        </TextBlock>
        <TextBlock x:Name="Relative_Time" HorizontalAlignment="Right" Margin="0,48,-3,0" TextWrapping="Wrap" Text="{Binding VM_Overall_time}" VerticalAlignment="Top" Height="28" Width="82" FontSize="20" d:LayoutOverrides="LeftMargin, RightMargin"/>
    </Grid>
</UserControl>
